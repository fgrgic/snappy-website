import { View, Text } from "react-native";
import React from "react";

type Props = {};

const Home = (props: Props) => {
  return (
    <View>
      <Text style={{ fontFamily: "nunito" }}>Home</Text>
    </View>
  );
};

export default Home;
